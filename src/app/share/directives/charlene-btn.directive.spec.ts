import { CharleneBtnDirective } from './charlene-btn.directive';

describe('CharleneBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CharleneBtnDirective();
    expect(directive).toBeTruthy();
  });
});
