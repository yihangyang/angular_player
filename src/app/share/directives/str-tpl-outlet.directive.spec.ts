import { StrTplOutletDirective } from './str-tpl-outlet.directive';

describe('StrTplOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new StrTplOutletDirective();
    expect(directive).toBeTruthy();
  });
});
