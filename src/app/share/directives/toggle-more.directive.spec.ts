import { ToggleMoreDirective } from './toggle-more.directive';

describe('ToggleMoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleMoreDirective();
    expect(directive).toBeTruthy();
  });
});
